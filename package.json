{
	"name": "kc-mev-back",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"typegen": "wrangler types --env-interface Env",
		"format": "biome check --write"
	},
	"dependencies": {
		"hono": "^4.7.4",
		"telegraf": "^4.16.3"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"@types/node": "^22.13.10",
		"wrangler": "^4.3.0"
	}
}
